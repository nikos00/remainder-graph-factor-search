# Generated output directory

This directory is intentionally empty before the rerun, except for this note.

The launcher creates one `exp_<digits>` directory per digit bucket and an `_run_logs` directory containing environment records, console logs, and checksums.

